c = CaosBox;
c.enviroment();
c.enviroment(false);

c.play;
c.bpm(10);
c.freqAnalyzer(true);

c.guiSettings(0.4,false);
c.gui(visible:true);

Window.browse
Window.allWindows
~w.visible
~w.alpha_(0.6);
~w.fullScreen(true)